# Medivet integration notes

- **Server env contract:** [MEDIVET_SERVER_ENV.md](./MEDIVET_SERVER_ENV.md) — variables for Resend email routes and filesystem blog CMS.
- **APIs:** `GET /api/medivet/blog`, `GET /api/medivet/blog/[slug]`, `POST /api/medivet/contact`, `POST /api/medivet/appointments` (JSON body: `name`, `email`, `message`).
- Align production names and inboxes with System Architect on Paperclip ticket **PAFAAA-8** before go-live.
